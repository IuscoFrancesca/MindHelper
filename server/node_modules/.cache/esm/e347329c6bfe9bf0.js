let express,mongoose,cors,readdirSync;_6bf‍.w("express",[["default",["express"],function(v){express=v}]]);_6bf‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6bf‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_6bf‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();

//database
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _6bf‍.g.console.log("DB connection error ", err));

//middlewares
app.use(
  express.json({
    limit: "5mb",
  })
);
app.use(express.urlencoded({ extended: true }));
app.use(cors());

//autoload routes
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT;
app.listen(port, () => console.log(`server running on port ${port}`));
